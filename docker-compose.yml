services:
  wwc2025:
    build: .
    container_name: wwc2025
    ports:
      - "8091:5000"
    # For development: live-edit markdown without rebuilding image
    volumes:
      - ./content:/app/content:ro
    restart: unless-stopped
